import {
  {{ upperCase name}},
} from './actions';

const initialState = {
  {{ camelCase name }}: [],
};

export default function (state = initialState, action) {
  switch (action.type) {
    case {{ upperCase name }}:
      return { ...state, {{ camelCase name}}: action.payload };
    default:
      return state;
  }
}
